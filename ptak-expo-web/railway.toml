[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
watchPatterns = ["ptak-expo-web/**"]

[deploy]
startCommand = "npx serve -s build -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 
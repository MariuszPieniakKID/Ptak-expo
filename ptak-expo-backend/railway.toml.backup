# Railway configuration for PTAK EXPO Backend
[build]
command = "npm install"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
PORT = "3001"
NODE_ENV = "production" 